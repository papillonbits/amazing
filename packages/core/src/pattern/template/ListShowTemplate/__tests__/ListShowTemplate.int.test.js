import { regular } from './ListShowTemplate.int.story'

describe('<ListShowTemplate />', () => {
  describe('Render', () => {
    test('must match regular()', () => {
      expect(global.renderToJSON(regular())).toMatchSnapshot()
    })
  })
})

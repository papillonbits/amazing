import { regular } from './InfoShowTemplate.int.story'

describe('<InfoShowTemplate />', () => {
  describe('Render', () => {
    test('must match regular()', () => {
      expect(global.renderToJSON(regular())).toMatchSnapshot()
    })
  })
})
